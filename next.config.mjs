/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
        domains: ['localhost'], // Añade 'localhost' para que puedas cargar imágenes desde tu servidor local
      },
};

export default nextConfig;
